# PayZen - Начните отсюда! 🚀

## Быстрая установка

### Вариант 1: Двойной клик (самый простой)

Просто дважды кликните на файл:
```
install.bat
```

### Вариант 2: Через PowerShell

Откройте PowerShell в папке проекта и выполните:

```powershell
.\setup.ps1
```

### Вариант 3: Через npm

```powershell
npm run setup
```

## Что произойдёт?

Скрипт автоматически:
1. ✅ Проверит наличие Node.js
2. ✅ Установит Node.js (если нужно)
3. ✅ Установит все зависимости проекта
4. ✅ Проверит структуру проекта

## После установки

1. **Создайте файл `.env`**:
   - Скопируйте `.env.example` в `.env`
   - Настройте подключение к базе данных

2. **Настройте PostgreSQL**:
   ```sql
   CREATE DATABASE payzen;
   ```

3. **Запустите проект**:

   **Терминал 1 - Backend:**
   ```powershell
   npm run server:dev
   ```

   **Терминал 2 - Frontend:**
   ```powershell
   npm run dev
   ```

4. **Откройте браузер**: http://localhost:3000

## Проблемы?

- **Ошибка "npm не найден"**: Запустите `.\install-node.ps1`
- **Ошибка "Execution Policy"**: Выполните:
  ```powershell
  Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
  ```
- **Нужны права администратора**: Скрипт запросит их автоматически

## Дополнительная информация

- 📖 **Полная инструкция**: `INSTALL.md`
- 📚 **Документация**: `README.md`
- 💡 **Концепция**: `CONCEPT.md`
- ⚡ **Быстрый старт**: `QUICKSTART.md`

---

**Готово!** Начните с `install.bat` или `.\setup.ps1`





