# PayZen - Инструкция по установке

## 🚀 Быстрая установка

### Автоматическая установка (рекомендуется)

Просто запустите скрипт настройки:

```powershell
.\setup.ps1
```

Этот скрипт автоматически:
1. ✅ Проверит наличие Node.js
2. ✅ Установит Node.js, если его нет
3. ✅ Установит все зависимости проекта
4. ✅ Проверит структуру проекта

### Ручная установка

Если автоматическая установка не работает:

#### Шаг 1: Установка Node.js

```powershell
.\install-node.ps1
```

Или установите вручную:
1. Откройте https://nodejs.org/
2. Скачайте LTS версию
3. Запустите установщик
4. **Важно**: Оставьте включённой опцию "Add to PATH"

#### Шаг 2: Установка зависимостей

```powershell
npm install
```

#### Шаг 3: Настройка окружения

1. Скопируйте `.env.example` в `.env`:
```powershell
Copy-Item .env.example .env
```

2. Откройте `.env` и настройте:
   - База данных PostgreSQL
   - JWT секрет
   - API URLs

#### Шаг 4: Настройка базы данных

1. Установите PostgreSQL (если ещё не установлен)
2. Создайте базу данных:
```sql
CREATE DATABASE payzen;
```

#### Шаг 5: Запуск проекта

**Терминал 1 - Backend:**
```powershell
npm run server:dev
```

**Терминал 2 - Frontend:**
```powershell
npm run dev
```

Откройте http://localhost:3000 в браузере.

## 🔧 Устранение проблем

### Ошибка: "npm не найден"

**Решение:**
1. Установите Node.js: `.\install-node.ps1`
2. Закройте и снова откройте PowerShell
3. Проверьте: `node --version` и `npm --version`

### Ошибка: "Execution Policy"

Если получаете ошибку о политике выполнения:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Затем снова запустите:
```powershell
.\setup.ps1
```

### Ошибка: "Требуются права администратора"

Скрипт установки Node.js требует прав администратора. Он автоматически запросит их при необходимости.

### Node.js установлен, но команды не работают

1. Закройте и снова откройте PowerShell
2. Проверьте PATH: `$env:Path`
3. Перезагрузите компьютер (в крайнем случае)

## 📋 Требования

- **Windows 10/11**
- **PowerShell 5.1+**
- **Интернет-соединение** (для скачивания Node.js)
- **Права администратора** (для установки Node.js)
- **PostgreSQL** (для базы данных)

## ✅ Проверка установки

После установки проверьте:

```powershell
# Проверка Node.js
node --version
# Должно показать: v20.x.x или выше

# Проверка npm
npm --version
# Должно показать: 10.x.x или выше

# Проверка зависимостей
npm list --depth=0
# Должен показать список установленных пакетов
```

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте, что все шаги выполнены
2. Убедитесь, что PowerShell запущен от имени администратора (для установки Node.js)
3. Проверьте логи ошибок
4. См. раздел "Устранение проблем" выше

---

**PayZen** - Умное управление подписками





