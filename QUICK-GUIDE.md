# PayZen - Быстрая инструкция

## ✅ Установка завершена!

Теперь нужно завершить настройку:

## 🚀 Быстрый старт (3 команды)

### 1. Исправить уязвимости и создать .env:
```powershell
npm run continue
```

### 2. Создать базу данных PostgreSQL:
```sql
CREATE DATABASE payzen;
```

### 3. Запустить проект:

**Терминал 1:**
```powershell
npm run server:dev
```

**Терминал 2:**
```powershell
npm run dev
```

Откройте: **http://localhost:3000**

---

## 📝 Подробная настройка

### Шаг 1: Исправить уязвимости

```powershell
npm audit fix
```

### Шаг 2: Создать .env файл

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
DB_NAME=payzen
DB_USER=postgres
DB_PASSWORD=ваш_пароль_postgres
DB_HOST=localhost
DB_PORT=5432

JWT_SECRET=измените-этот-секретный-ключ-минимум-32-символа

API_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001

PORT=3001
NODE_ENV=development
```

Или скопируйте из `env.example.txt`:
```powershell
Copy-Item env.example.txt .env
```

Затем откройте `.env` и измените:
- `DB_PASSWORD` - ваш пароль PostgreSQL
- `JWT_SECRET` - любой секретный ключ (минимум 32 символа)

### Шаг 3: Установить PostgreSQL

Если PostgreSQL не установлен:

1. Скачайте с https://www.postgresql.org/download/windows/
2. Установите (запомните пароль для `postgres`)
3. Или используйте Docker:
   ```powershell
   docker run --name postgres-payzen -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres
   ```

### Шаг 4: Создать базу данных

**Через psql:**
```powershell
psql -U postgres -c "CREATE DATABASE payzen;"
```

**Или через pgAdmin:**
- Откройте pgAdmin
- Создайте базу данных с именем `payzen`

### Шаг 5: Запустить проект

**Терминал 1 (Backend):**
```powershell
npm run server:dev
```

**Терминал 2 (Frontend):**
```powershell
npm run dev
```

### Шаг 6: Открыть в браузере

http://localhost:3000

---

## ✅ Готово!

Теперь вы можете:
- Зарегистрировать аккаунт
- Добавить подписки
- Использовать все функции PayZen

---

**Нужна помощь?** См. `GETTING-STARTED.md` или `README.md`





