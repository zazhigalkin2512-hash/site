# PayZen - Как запустить

## 🚀 Самый простой способ

### Просто дважды кликните на файл:

```
RUN-THIS.bat
```

Или:

```
START-HERE.bat
```

Это автоматически:
- ✅ Создаст файл `.env` (если нужно)
- ✅ Проверит PostgreSQL
- ✅ Создаст базу данных `payzen` (если нужно)
- ✅ Запустит backend сервер (порт 3001)
- ✅ Запустит frontend сервер (порт 3000)

Затем откройте: **http://localhost:3000**

---

## 📋 Альтернативные способы

### Через PowerShell:

```powershell
.\start.ps1
```

### Через npm:

```powershell
npm start
```

---

## ⚙️ Что нужно установить

### 1. PostgreSQL (обязательно)

**Вариант A: Установщик**
1. Скачайте с https://www.postgresql.org/download/windows/
2. Установите (пароль по умолчанию: `postgres`)
3. Запомните пароль!

**Вариант B: Docker**
```powershell
docker run --name postgres-payzen -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres
```

### 2. Настройка .env

Скрипт создаст `.env` автоматически, но если нужно изменить:

Откройте `.env` и измените:
- `DB_PASSWORD` - ваш пароль PostgreSQL
- `JWT_SECRET` - любой секретный ключ (минимум 32 символа)

---

## 🔧 Если что-то не работает

### Ошибка: "ECONNREFUSED"

**Проблема:** PostgreSQL не запущен

**Решение:**
1. Откройте "Службы" (Services)
2. Найдите "postgresql-x64-XX" (или похожее)
3. Нажмите "Запустить" (Start)

Или через PowerShell:
```powershell
Get-Service postgresql* | Start-Service
```

### Ошибка: "Database does not exist"

**Проблема:** База данных не создана

**Решение:**
```powershell
psql -U postgres -c "CREATE DATABASE payzen;"
```

### Ошибка: "Password authentication failed"

**Проблема:** Неправильный пароль в `.env`

**Решение:**
1. Откройте `.env`
2. Измените `DB_PASSWORD` на правильный пароль PostgreSQL

---

## ✅ Проверка

После запуска проверьте:

1. **Backend работает:** http://localhost:3001/api/health
   - Должен показать: `{"status":"ok","service":"PayZen API"}`

2. **Frontend работает:** http://localhost:3000
   - Должна открыться главная страница PayZen

---

## 🎉 Готово!

Теперь вы можете:
- Зарегистрировать аккаунт
- Добавить подписки
- Использовать все функции PayZen

---

**Просто запустите `RUN-THIS.bat` и всё заработает!** 🚀





