# PayZen - Начало работы

## ✅ Установка завершена!

Отлично! Все зависимости установлены. Теперь нужно завершить настройку проекта.

## 🔧 Следующие шаги

### 1. Исправить уязвимости (опционально)

```powershell
npm audit fix
```

Или запустите автоматический скрипт:
```powershell
npm run continue
```

### 2. Создать файл .env

Скопируйте `.env.example` в `.env`:

```powershell
Copy-Item .env.example .env
```

Или запустите:
```powershell
npm run continue
```

Затем откройте `.env` и настройте:
- **DB_PASSWORD** - пароль для PostgreSQL
- **JWT_SECRET** - секретный ключ (минимум 32 символа)

### 3. Установить PostgreSQL

Если PostgreSQL ещё не установлен:

**Вариант A: Скачать установщик**
1. Откройте https://www.postgresql.org/download/windows/
2. Скачайте и установите PostgreSQL
3. Запомните пароль для пользователя `postgres`

**Вариант B: Использовать Docker**
```powershell
docker run --name postgres-payzen -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres
```

### 4. Создать базу данных

**Через psql:**
```powershell
psql -U postgres -c "CREATE DATABASE payzen;"
```

**Или через pgAdmin:**
1. Откройте pgAdmin
2. Подключитесь к серверу
3. Правый клик на "Databases" → "Create" → "Database"
4. Имя: `payzen`
5. Нажмите "Save"

**Или через SQL:**
```sql
CREATE DATABASE payzen;
```

### 5. Запустить проект

**Терминал 1 - Backend сервер:**
```powershell
npm run server:dev
```

**Терминал 2 - Frontend сервер:**
```powershell
npm run dev
```

### 6. Открыть в браузере

Откройте: **http://localhost:3000**

## 🚀 Быстрый старт (всё автоматически)

Просто запустите:
```powershell
npm run continue
```

Этот скрипт:
- ✅ Исправит уязвимости npm
- ✅ Создаст файл .env
- ✅ Проверит PostgreSQL
- ✅ Покажет инструкции

## 📋 Проверка установки

### Проверить Node.js:
```powershell
node --version
npm --version
```

### Проверить PostgreSQL:
```powershell
psql --version
```

### Проверить подключение к БД:
```powershell
psql -U postgres -d payzen -c "SELECT version();"
```

## ❓ Проблемы?

### Ошибка подключения к базе данных

1. Проверьте, что PostgreSQL запущен
2. Проверьте настройки в `.env`
3. Проверьте пароль пользователя `postgres`

### Ошибка "Database does not exist"

Создайте базу данных:
```sql
CREATE DATABASE payzen;
```

### Порт уже занят

Измените порт в `.env`:
```env
PORT=3002
API_URL=http://localhost:3002
NEXT_PUBLIC_API_URL=http://localhost:3002
```

## 📚 Дополнительная информация

- **Полная документация**: `README.md`
- **Концепция проекта**: `CONCEPT.md`
- **Инструкции по установке**: `INSTALL.md`

---

**Готово!** После настройки базы данных запустите серверы и начните разработку! 🎉





