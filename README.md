# PayZen - Умное управление подписками

PayZen — это современное веб-приложение для автоматического отслеживания всех ваших регулярных платежей за цифровые сервисы: стриминг, облачные хранилища, софт, онлайн-курсы, SaaS-инструменты и другие подписки.

## 🚀 Основные функции

### Автоматическое обнаружение подписок
- **Email интеграция**: Подключите email для анализа платёжных писем
- **Банковские API**: Интеграция с Plaid, Tink для распознавания регулярных списаний
- **Ручной ввод**: Добавление подписок вручную или сканирование чеков

### Умная категоризация и анализ
- Автоматическая группировка по категориям (Развлечения, Образование, Работа, Здоровье, Финансы и т.д.)
- Ежемесячные отчёты с анализом трат
- Обнаружение неиспользуемых подписок (60+ дней без использования)
- Выявление дублирующих сервисов

### Уведомления и контроль
- Напоминания за 3–7 дней до списания
- Оповещения о дублирующих сервисах
- Рекомендации по оптимизации расходов

### Премиум-функции
- Неограниченное число подписок
- Экспорт отчётов (PDF, Excel)
- Приоритетные уведомления
- Прогноз бюджета на следующий месяц

## 🛠 Технологии

- **Frontend**: Next.js 14, React 18, TypeScript, Tailwind CSS
- **Backend**: Node.js, Express, Sequelize ORM
- **База данных**: PostgreSQL
- **UI компоненты**: Lucide React, Recharts
- **Аутентификация**: JWT

## 📦 Установка

### 🚀 Автоматическая установка (рекомендуется)

Просто запустите скрипт настройки - он автоматически установит Node.js (если нужно) и все зависимости:

```powershell
.\setup.ps1
```

Или используйте npm:
```powershell
npm run setup
```

### 📝 Ручная установка

1. **Установка Node.js** (если ещё не установлен):
```powershell
.\install-node.ps1
```
Или установите вручную с https://nodejs.org/ (требуется версия 18+)

2. **Установка зависимостей:**
```powershell
npm install
```

> **Примечание**: При выполнении `npm install` автоматически проверяется наличие Node.js. Если его нет, запустится скрипт установки.

3. Создайте файл `.env` в корне проекта:
```env
# Database
DB_NAME=payzen
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# JWT
JWT_SECRET=your-secret-key-change-in-production

# API
API_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001

# Email (для IMAP)
# IMAP_HOST=imap.gmail.com
# IMAP_PORT=993

# Bank Integrations (опционально)
# PLAID_CLIENT_ID=your_plaid_client_id
# PLAID_SECRET=your_plaid_secret
```

4. Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE payzen;
```

5. Запустите сервер разработки:
```bash
# Терминал 1: Backend сервер
npm run server:dev

# Терминал 2: Frontend сервер
npm run dev
```

6. Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📁 Структура проекта

```
payzen/
├── app/                    # Next.js App Router
│   ├── page.tsx           # Главная страница
│   ├── login/             # Страница входа
│   ├── register/          # Страница регистрации
│   └── dashboard/          # Дашборд
│       ├── page.tsx       # Главный дашборд
│       ├── subscriptions/ # Управление подписками
│       └── integrations/  # Интеграции
├── components/            # React компоненты
│   ├── DashboardStats.tsx
│   ├── SubscriptionList.tsx
│   ├── MonthlyReport.tsx
│   └── ...
├── server/                # Backend сервер
│   ├── config/           # Конфигурация
│   ├── models/           # Sequelize модели
│   ├── routes/           # API маршруты
│   ├── services/         # Бизнес-логика
│   └── middleware/       # Middleware
└── package.json
```

## 🔐 Безопасность

- Все пароли хешируются с помощью bcrypt
- JWT токены для аутентификации
- Данные пользователей шифруются
- Полная прозрачность использования данных

## 📊 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Подписки
- `GET /api/subscriptions` - Список подписок
- `POST /api/subscriptions` - Создать подписку
- `PUT /api/subscriptions/:id` - Обновить подписку
- `DELETE /api/subscriptions/:id` - Удалить подписку
- `GET /api/subscriptions/upcoming` - Предстоящие списания

### Аналитика
- `GET /api/analytics/stats` - Статистика дашборда
- `GET /api/analytics/monthly-report` - Ежемесячный отчёт
- `GET /api/analytics/category-breakdown` - Разбивка по категориям

### Интеграции
- `GET /api/integrations` - Список интеграций
- `POST /api/integrations/email` - Подключить email
- `POST /api/integrations/bank` - Подключить банк
- `POST /api/integrations/:id/sync` - Синхронизировать
- `DELETE /api/integrations/:id` - Удалить интеграцию

### Уведомления
- `GET /api/notifications` - Список уведомлений
- `PUT /api/notifications/:id/read` - Отметить как прочитанное
- `PUT /api/notifications/read-all` - Отметить все как прочитанные

### Премиум
- `GET /api/premium/status` - Статус премиум
- `POST /api/premium/activate` - Активировать премиум
- `POST /api/premium/cancel` - Отменить премиум
- `GET /api/premium/export` - Экспорт отчёта

## 🎨 Дизайн

Приложение использует минималистичный дизайн в стиле "финансовый wellness":
- Чистый интерфейс
- Интуитивная навигация
- Адаптивный дизайн для мобильных устройств
- Современная цветовая палитра

## 📝 Лицензия

Этот проект создан для демонстрации концепции PayZen.

## 🤝 Вклад

Приветствуются любые предложения и улучшения!

---

**PayZen** - Умное управление подписками. Экономьте деньги, контролируйте расходы.


