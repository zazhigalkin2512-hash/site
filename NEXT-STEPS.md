# PayZen - Следующие шаги

## ✅ После установки Node.js

Если Node.js был только что установлен, выполните одно из следующего:

### Вариант 1: Автоматически (рекомендуется)

Просто запустите снова:
```powershell
.\setup.ps1
```

Или:
```powershell
npm run setup
```

### Вариант 2: Перезапуск PowerShell

1. **Закройте** текущее окно PowerShell
2. **Откройте** новое окно PowerShell
3. Перейдите в папку проекта:
   ```powershell
   cd C:\Users\Lenovo\Desktop\сайт
   ```
4. Запустите установку:
   ```powershell
   .\setup.ps1
   ```

### Вариант 3: Через install.bat

Просто дважды кликните на файл:
```
install.bat
```

## 🔍 Проверка установки Node.js

После установки проверьте:

```powershell
node --version
npm --version
```

Обе команды должны показать версии.

## 📦 Установка зависимостей

После того, как Node.js установлен и доступен:

```powershell
npm install
```

## 🚀 Запуск проекта

После успешной установки всех зависимостей:

**Терминал 1 - Backend:**
```powershell
npm run server:dev
```

**Терминал 2 - Frontend:**
```powershell
npm run dev
```

Откройте http://localhost:3000 в браузере.

## ❓ Проблемы?

### Node.js не найден после установки

1. Закройте PowerShell
2. Откройте новое окно PowerShell
3. Проверьте: `node --version`

### Ошибка "npm не найден"

Убедитесь, что Node.js установлен правильно:
- Проверьте `C:\Program Files\nodejs\`
- Должны быть файлы: `node.exe` и `npm.cmd`

### Ошибка установки зависимостей

Попробуйте:
```powershell
npm cache clean --force
npm install
```

---

**Готово!** После установки Node.js просто запустите `.\setup.ps1` снова.





